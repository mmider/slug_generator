name: slug_generator
description: A generator for random URL slugs
version: 0.1.0
repository: https://github.com/mmider/slug_generator

environment:
  sdk: ">=2.19.6 <3.0.0"

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
